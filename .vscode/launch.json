{
    "gopls": {
        "experimentalWorkspaceModule": true
    },
    "configurations": [
        {
            "name": "Launch file",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "cwd": "${workspaceFolder}",
            "program": "${file}"
        }
    ]
}